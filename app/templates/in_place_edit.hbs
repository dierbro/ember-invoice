{{#if view.isEditing}}
    {{view view.inputField valueBinding="view.content"}}
{{else}}
    {{#if view.isEmpty}}
        {{view.emptyValue}}
    {{else}}
         {{view.content}}
    {{/if}}
{{/if}}
