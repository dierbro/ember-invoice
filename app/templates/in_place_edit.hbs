{{#if view.isEditing}}
    {{view view.inputField valueBinding="view.content" class="form-control"}}
{{else}}
    {{#if view.isEmpty}}
        {{view.emptyValue}}
    {{else}}
      {{yield}}
    {{/if}}
{{/if}}
